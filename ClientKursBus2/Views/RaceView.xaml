<UserControl x:Class="ClientKursBus2.Views.RaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientKursBus2.Views"
             xmlns:vm="clr-namespace:ClientKursBus2.ViewModels"
             xmlns:Menu="clr-namespace:ClientKursBus2.Utills"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Style="{StaticResource Page_Style}">
    <UserControl.DataContext>
        <vm:RaceViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Border CornerRadius="0,20,20,0" Margin="0,5,5,0">
            <Grid Margin="15">
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="9*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource ToolBarButtonBaseStyle}"
                            Command="{Binding AddCommand}" Width="30"
                            Height="30" HorizontalAlignment="Right">
                        <Image Source="/Assets/add.png"/>
                    </Button>
                    <Button Style="{StaticResource ToolBarButtonBaseStyle}"
                         Command="{Binding DeleteCommand}" CommandParameter="{Binding Selected}"
                        Width="30"
                        Height="30" HorizontalAlignment="Right">
                        <Image Source="/Assets/Del.png"/>
                    </Button>
                </StackPanel>
                
                <DataGrid ItemsSource="{Binding RaceList}"
                          AutoGenerateColumns="False"
                          Grid.Row="1" IsReadOnly="True"
                          SelectedItem="{Binding Selected}" FontSize="14">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction
                                Command="{Binding EditCommand}"
                                CommandParameter="{Binding Selected}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Изменить" 
                                      Command="{Binding EditCommand}"
                                      CommandParameter="{Binding Selected}"/>
                            <MenuItem Header="Удалить" 
                                      Command="{Binding DeleteCommand}"
                                      CommandParameter="{Binding Selected}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Номер рейса" Width="Auto" Binding="{Binding RaceNumber}"/>
                        <DataGridTextColumn Header="Маршрут" Width="Auto" Binding="{Binding TripId}"/>
                        <DataGridTextColumn Header="Дата" Width="Auto" Binding="{Binding RaceData}"/>
                        <DataGridTextColumn Header="Время" Width="Auto" Binding="{Binding RaceTime}"/>
                        <DataGridTextColumn Header="Билеты" Width="Auto" Binding="{Binding Load}"/>
                        <DataGridTextColumn Header="Прибыль" Width="Auto" Binding="{Binding Profit}"/>
                        <DataGridTextColumn Header="Загруженность" Width="Auto" Binding="{Binding Circulation}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
